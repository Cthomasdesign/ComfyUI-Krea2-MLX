# The MLX Krea-2 pipeline (krea2_engine/) is vendored in this repo — no external git dependency.
# These are its own inference deps.
mlx-metal>=0.31,<0.32
mflux>=0.18,<0.19
transformers>=5.0,<6
huggingface_hub>=0.26,<2
requests>=2.28,<3
numpy>=1.24,<3
pillow>=10,<13
